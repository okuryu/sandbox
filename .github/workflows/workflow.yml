on:
  - push
  - pull_request
jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node:
          - 8
          - 10
          - 12
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
